<%= simple_form_for @word do |f| %>
  <%= f.input :title, label: "Слово", required: true %>
  <%= f.input :translation, label: "Перевод", required: true %>
  <%= f.input :example, label: "Пример, примечание" %>
  <%= f.button :submit, 'Добавить', class: "btn btn-success" %>
<% end %>
